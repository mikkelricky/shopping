version: "2"

# @see https://docker-sync.readthedocs.io/en/latest/getting-started/configuration.html
syncs:
    shopping-sync:
        sync_strategy: 'native_osx'
        src: '.'
        host_disk_mount_mode: 'cached' # see https://docs.docker.com/docker-for-mac/osxfs-caching/#cached
